import ProjectMediaCarousel from "../project-detail/ProjectMediaCarousel";

export default function DineEaseCarousel({ slides }) {
  return <ProjectMediaCarousel slides={slides} projectName="DineEase" sourceNote="Product reference captured from the supplied DineEase feature guide" />;
}
